# Comments Feature - Architecture Diagram

## 📐 System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                               │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │            PostDetailPage (page.tsx)                           │ │
│  │  ┌──────────────────────────────────────────────────────────┐ │ │
│  │  │  Sticky Post (Discussion/Poll/Event)                     │ │ │
│  │  └──────────────────────────────────────────────────────────┘ │ │
│  │  ┌──────────────────────────────────────────────────────────┐ │ │
│  │  │  CommentsSection Component                               │ │ │
│  │  │  ┌────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  Add Comment Button & Input                        │ │ │ │
│  │  │  └────────────────────────────────────────────────────┘ │ │ │
│  │  │  ┌────────────────────────────────────────────────────┐ │ │ │
│  │  │  │  CommentComponent (Top-level)                      │ │ │ │
│  │  │  │  ├─ User Avatar & Name                             │ │ │ │
│  │  │  │  ├─ Comment Text                                   │ │ │ │
│  │  │  │  ├─ Reply Button                                   │ │ │ │
│  │  │  │  ├─ Reply Input (collapsible)                      │ │ │ │
│  │  │  │  └─ Nested Replies:                                │ │ │ │
│  │  │  │     ┌────────────────────────────────────────────┐ │ │ │ │
│  │  │  │     │  CommentComponent (Reply - depth 1)        │ │ │ │ │
│  │  │  │     │  └─ CommentComponent (depth 2)             │ │ │ │ │
│  │  │  │     │     └─ CommentComponent (depth 3)          │ │ │ │ │
│  │  │  │     │        └─ ... (up to depth 5)              │ │ │ │ │
│  │  │  │     └────────────────────────────────────────────┘ │ │ │ │
│  │  │  └────────────────────────────────────────────────────┘ │ │ │
│  │  └──────────────────────────────────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                ↓ ↑
                    User Actions / UI Updates
                                ↓ ↑
┌─────────────────────────────────────────────────────────────────────┐
│                      STATE MANAGEMENT LAYER                          │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       useCommentsStore (Zustand)                               │ │
│  │                                                                │ │
│  │  State:                                                        │ │
│  │    commentsByPost: { [postId]: Comment[] }                    │ │
│  │    loading: boolean                                           │ │
│  │    error: string | null                                       │ │
│  │    success: string | null                                     │ │
│  │                                                                │ │
│  │  Actions:                                                      │ │
│  │    • loadCommentsForPost(postId)                              │ │
│  │    • addCommentToPost(postId, text, userId, userName)         │ │
│  │    • addReplyToComment(postId, parentId, text, ...)          │ │
│  │    • getCommentsForPost(postId)                               │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                ↓ ↑
                         API Calls / Responses
                                ↓ ↑
┌─────────────────────────────────────────────────────────────────────┐
│                          API CLIENT LAYER                            │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       commentsApis.ts                                          │ │
│  │                                                                │ │
│  │    • getPostComments(postId)                                  │ │
│  │         → GET /api/comments/post/:postId                      │ │
│  │                                                                │ │
│  │    • addComment(postId, text, userId)                         │ │
│  │         → POST /api/comments/post/:postId                     │ │
│  │                                                                │ │
│  │    • addReply(postId, parentId, text, userId)                 │ │
│  │         → POST /api/comments/:commentId/reply                 │ │
│  │                                                                │ │
│  │    • updateComment(commentId, text)                           │ │
│  │         → PUT /api/comments/:commentId                        │ │
│  │                                                                │ │
│  │    • deleteComment(commentId)                                 │ │
│  │         → DELETE /api/comments/:commentId                     │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
                                ↓ ↑
                          HTTP Requests
                                ↓ ↑
┌─────────────────────────────────────────────────────────────────────┐
│                         BACKEND (TO IMPLEMENT)                       │
│                                                                       │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       Express Routes (comments.js)                             │ │
│  │    • GET    /api/comments/post/:postId                        │ │
│  │    • POST   /api/comments/post/:postId                        │ │
│  │    • POST   /api/comments/:commentId/reply                    │ │
│  │    • PUT    /api/comments/:commentId                          │ │
│  │    • DELETE /api/comments/:commentId                          │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                ↓ ↑                                   │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       Controllers (commentsController.js)                      │ │
│  │    • getPostComments(req, res)                                │ │
│  │    • addComment(req, res)                                     │ │
│  │    • addReply(req, res)                                       │ │
│  │    • updateComment(req, res)                                  │ │
│  │    • deleteComment(req, res)                                  │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                ↓ ↑                                   │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       Sequelize Model (commentsOrm.js)                         │ │
│  │    • commentId: STRING (unique)                               │ │
│  │    • postId: STRING (FK → group_posts.post_id)                │ │
│  │    • userId: STRING (FK → users.user_id)                      │ │
│  │    • parentCommentId: STRING (FK → comments.comment_id, NULL) │ │
│  │    • commentText: TEXT                                        │ │
│  │    • isDeleted: BOOLEAN                                       │ │
│  │    • createdAt: TIMESTAMP                                     │ │
│  │    • updatedAt: TIMESTAMP                                     │ │
│  └────────────────────────────────────────────────────────────────┘ │
│                                ↓ ↑                                   │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │       MySQL Database (comments table)                          │ │
│  │                                                                │ │
│  │    Table: comments                                            │ │
│  │    ├─ id (PK, AUTO_INCREMENT)                                 │ │
│  │    ├─ comment_id (UNIQUE)                                     │ │
│  │    ├─ post_id (FK, INDEX)                                     │ │
│  │    ├─ user_id (FK, INDEX)                                     │ │
│  │    ├─ parent_comment_id (FK, INDEX, NULLABLE)                 │ │
│  │    ├─ comment_text (TEXT)                                     │ │
│  │    ├─ is_deleted (BOOLEAN, DEFAULT false)                     │ │
│  │    ├─ created_at (TIMESTAMP)                                  │ │
│  │    └─ updated_at (TIMESTAMP)                                  │ │
│  │                                                                │ │
│  └────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Example: Adding a Comment

```
┌────────────────────────────────────────────────────────────────────┐
│ STEP 1: User types comment and clicks "Post Comment"              │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 2: CommentsSection.handleSubmitComment() called              │
│         → onAddComment(commentText) passed to parent               │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 3: PostDetailPage.handleAddComment() called                  │
│         → Gets userId and userName from useUser()                  │
│         → Calls store.addCommentToPost(postId, text, user, name)  │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 4: Store.addCommentToPost() executes                         │
│         → Sets loading = true                                      │
│         → Calls API: addComment(postId, text, userId)             │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 5: API Client sends HTTP POST request                        │
│         → POST /api/comments/post/:postId                          │
│         → Body: { commentText, userId }                            │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 6: Backend receives request (if implemented)                 │
│         → Controller validates data                                │
│         → Creates comment in database via Sequelize                │
│         → Returns response with commentId and timestamps           │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 7: Store receives response                                   │
│         → Creates Comment object with server data                  │
│         → Updates commentsByPost[postId] array                     │
│         → Sets loading = false, success = true                     │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 8: UI automatically re-renders (Zustand subscription)        │
│         → PostDetailPage.comments = getCommentsForPost(postId)     │
│         → CommentsSection receives updated comments prop           │
│         → New comment appears in the list                          │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Example: Adding a Reply

```
┌────────────────────────────────────────────────────────────────────┐
│ STEP 1: User clicks "Reply" on a comment                          │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 2: CommentComponent shows reply input box                    │
│         → User types reply and clicks "Reply"                      │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 3: CommentComponent.handleReplySubmit() called               │
│         → onReply(commentId, replyText) passed to parent           │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 4: PostDetailPage.handleReplyToComment() called              │
│         → Gets userId and userName from useUser()                  │
│         → Calls store.addReplyToComment(postId, parentId, ...)    │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 5: Store.addReplyToComment() executes                        │
│         → Calls API: addReply(postId, parentId, text, userId)     │
│         → Uses recursive function to find parent comment           │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 6: Recursive function traverses comment tree                 │
│         → Searches for comment where commentId === parentId        │
│         → When found, adds new reply to that comment's replies[]   │
│         → If not found at current level, searches nested replies   │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 7: Store updates commentsByPost                              │
│         → Comment tree is updated with new nested reply            │
│         → Sets success = true                                      │
└────────────────────────────────────────────────────────────────────┘
                                ↓
┌────────────────────────────────────────────────────────────────────┐
│ STEP 8: UI automatically re-renders                               │
│         → Reply appears nested under parent comment                │
│         → Indented with left border                                │
│         → depth prop increases for child component                 │
└────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Component Hierarchy

```
PostDetailPage
└── CommentsSection
    └── CommentComponent (depth=0)
        ├── Avatar
        ├── Comment Header (name, timestamp, collapse button)
        ├── Comment Text
        ├── Reply Button
        ├── Reply Input Box (collapsible)
        └── Replies Container
            └── CommentComponent (depth=1) ← RECURSIVE
                └── CommentComponent (depth=2)
                    └── CommentComponent (depth=3)
                        └── CommentComponent (depth=4)
                            └── CommentComponent (depth=5) [MAX]
```

---

## 🗃️ Database Relationships

```
┌─────────────────┐
│  group_posts    │
│  ─────────────  │
│  post_id (PK)   │──┐
│  ...            │  │
└─────────────────┘  │
                     │
                     │ 1:N (one post has many comments)
                     │
                     ↓
           ┌─────────────────┐
           │    comments     │
           │  ─────────────  │
           │  comment_id (PK)│──┐
           │  post_id (FK)   │←─┘
           │  user_id (FK)   │←─────────┐
           │  parent_cmt_id  │───┐      │
           │  comment_text   │   │      │
           │  is_deleted     │   │      │
           │  created_at     │   │      │
           │  updated_at     │   │      │
           └─────────────────┘   │      │
                     ↑            │      │
                     │            │      │
                     └────────────┘      │
                     Self-reference      │
                     (for nested replies)│
                                         │
┌─────────────────┐                     │
│     users       │                     │
│  ─────────────  │                     │
│  user_id (PK)   │─────────────────────┘
│  ...            │   1:N (one user has many comments)
└─────────────────┘
```

---

## 📊 State Structure in Store

```typescript
{
  commentsByPost: {
    "post-123": [
      {
        commentId: "comment-1",
        postId: "post-123",
        userId: "user-1",
        userName: "john",
        commentText: "Great post!",
        createdAt: "2025-10-31T12:00:00Z",
        updatedAt: "2025-10-31T12:00:00Z",
        replies: [
          {
            commentId: "reply-1",
            userId: "user-2",
            userName: "jane",
            commentText: "Thanks!",
            createdAt: "2025-10-31T12:05:00Z",
            updatedAt: "2025-10-31T12:05:00Z",
            replies: [
              {
                commentId: "reply-2",
                userId: "user-1",
                userName: "john",
                commentText: "You're welcome!",
                createdAt: "2025-10-31T12:10:00Z",
                updatedAt: "2025-10-31T12:10:00Z",
                replies: []
              }
            ]
          }
        ]
      }
    ],
    "post-456": [ /* ... */ ]
  },
  loading: false,
  error: null,
  success: "Comment added successfully"
}
```

---

## 🎯 Key Design Decisions

### 1. **Zustand for State Management**
- ✅ Consistent with your existing stores
- ✅ Simple API, no boilerplate
- ✅ Built-in devtools support
- ✅ Automatic re-renders on state change

### 2. **Organized by Post ID**
- ✅ Fast lookups: O(1) access to comments for any post
- ✅ Efficient memory: Only load comments for viewed posts
- ✅ Easy cleanup: Can remove old post's comments from memory

### 3. **Recursive Component Structure**
- ✅ Unlimited nesting depth in data structure
- ✅ Limited to 5 levels in UI (for UX)
- ✅ Clean, maintainable code
- ✅ Same component handles all depth levels

### 4. **Optimistic Updates**
- ✅ Instant UI feedback
- ✅ Works without backend
- ✅ Falls back gracefully on API errors
- ✅ Great for development and testing

### 5. **Flat Database Structure**
- ✅ Simple queries
- ✅ Easy to maintain
- ✅ Can be transformed to nested structure in code
- ✅ Efficient indexing

---

## 📝 File Dependencies

```
page.tsx
  ├── imports: useCommentsStore
  ├── imports: CommentsSection
  └── uses: user from useAuth

commentsStore.tsx
  ├── imports: create (zustand)
  ├── imports: devtools (zustand/middleware)
  └── imports: API functions from commentsApis.ts

commentsApis.ts
  └── uses: fetch API (built-in)

commentsSection.tsx
  ├── imports: CommentComponent
  └── receives: comments, onAddComment, onReplyToComment

commentComponent.tsx
  └── receives: comment data, depth, onReply
  └── renders: self (recursively for replies)
```

---

This architecture provides a **clean separation of concerns**, follows **React/Next.js best practices**, and is **consistent with your existing codebase patterns**.

